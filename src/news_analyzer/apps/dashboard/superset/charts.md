# Suggested Charts

- Top entities by frequency over time (`entities.label`, `published_at`).
- Class distribution by source (`class_label`, `source_type`).
- News throughput by hour (`published_at`).
- Summary coverage ratio (`summary_status`).
